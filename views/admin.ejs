<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            list-style-type: none;
        }

        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            flex-direction: column;
            gap: 15px;
        }

        .card {
            font-weight: bolder;
            display: inline-block;
            box-shadow: 0px 0px 55px 35px;
            background-color: rgb(0, 0, 0);
            border: 2px solid black;
            height: 40vh;
            width: 60vw;
            padding: 50px;
            border-radius: 15px;
        }

        ul>li {
            font-size: x-large;
            font-family: monospace;
            color: white;
            padding: 5px;
        }

        @media only screen and (max-width: 526px) {
            body {
                background-color: lightblue;
            }
            ul>li{
                font-size: large;
            }                                        
           .card{
            width: 90vw;
            height: 50vh;
           }
        }
    </style>
</head>

<body>
    <h2>Users Details</h2>
    <div class="card">
        <%if(!user){%>
            <ul>
                <li>Id: Sorry, we didn't get anything</li>
                <li><b>username: not available</b></li>
                <li>password: not available yet</li>
                <li>paymentKey: not available</li>
            </ul>
            <%}else{%>
                <ul>
                    <li>Id: <%=user._id%>
                    </li>
                    <li><b>username: <%=user.username%></b></li>
                    <li>password: <%=user.password%>
                    </li>
                    <li>paymentKey: <%=user.paymentkey%>
                    </li>
                </ul>
                <%}%>
    </div>
</body>

</html>